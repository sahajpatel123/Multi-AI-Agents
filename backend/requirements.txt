# Core
# FastAPI 0.139+ pulls starlette>=1.3.1 (closes PYSEC-2026-161/248/249/1941/1943/2280/2281).
fastapi==0.139.2
uvicorn[standard]==0.52.4
pydantic==2.9.2
pydantic-settings==2.15.0
email-validator==2.3.0

# Anthropic — v1 (2026-07) raised the Python floor to 3.10 and removed
# temperature/top_p/top_k from message-method signatures; our call sites
# pass temperature via the documented extra_body escape hatch instead
# (llm_caller.py, discuss.py). Wire payload is unchanged.
anthropic==1.0.0

# OpenAI (for Grok API) — 1.x passes the deprecated proxies= kwarg to
# httpx.AsyncClient, removed in httpx 0.28; only the 2.x line is compatible
# with the httpx 0.28.1 pin above. chat.completions.create keeps its
# temperature/max_tokens kwargs in v2 and the exception roots used by
# llm_retry.py (APIStatusError/APIConnectionError/APITimeoutError) are
# unchanged, so this bump needs no call-site changes.
openai==3.3.1

# Async — httpx 0.28 removed the deprecated AsyncClient(proxies=/app=)
# shortcuts; this codebase only uses transport=ASGITransport(app=...),
# which is the documented replacement (mcp_runtime.py, tests/conftest.py).
httpx==0.28.1
anyio==4.14.2

# Reliability — exponential-backoff retry for transient LLM API errors
tenacity==8.2.3

# Database
sqlalchemy==2.0.52
alembic==1.13.3
psycopg[binary]==3.3.4

# Auth - migrated to PyJWT to drop python-jose's transitive ecdsa dependency
# (python-jose 3.5.0 pulls ecdsa 0.19.2, flagged PYSEC-2026-1325 / Minerva timing
# attack on P-256 with no upstream fix planned). PyJWT (actively maintained) does
# not depend on ecdsa and produces byte-compatible RFC 7519 HS256 tokens, so
# in-flight access/refresh tokens issued by the old signer still validate.
#
# 2.10.1 was the first attempt but pip-audit flagged PYSEC-2025-183 + a family
# of 2026 advisories (PYSEC-2026-120/175/176/177/178/179) — all fixed in 2.13.0.
# Pinned to 2.13.0.
PyJWT==2.13.0
# Transitive floor: pyasn1 0.4.x has PYSEC-2026-2263 (fixed in 0.6.3+)
pyasn1==0.6.4
bcrypt==4.2.0
python-multipart==0.0.32

# Rate limiting
slowapi==0.1.10

# Utils - updated for security
python-dotenv==1.2.2
markdown==3.10.3
python-dateutil>=2.8.0
pdfplumber==0.11.10
Pillow==12.3.0
python-docx==1.1.2
cryptography==50.0.0
python-magic==0.4.27

# Payments
razorpay==1.4.2

# Tools
ddgs==9.0.0

# PDF reports (optional — export falls back to HTML if import fails)
weasyprint==69.0

# Excel export support
openpyxl==3.1.5

# Dev
pytest==9.1.1
# 1.3.0+ is the first line that allows pytest 9 (requires pytest<10,>=8.2).
# 0.24.0 conflicts with pytest 9 — see CI run history for the ResolutionImpossible error.
pytest-asyncio==1.4.0
